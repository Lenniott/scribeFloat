{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "onboarding",
  "description": "Onboarding satellite — permissions + complete + show main",
  "windows": ["onboarding"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "onboarding"
  ]
}
