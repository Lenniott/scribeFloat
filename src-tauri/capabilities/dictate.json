{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dictate",
  "description": "Dictate satellite — minimal app commands + window chrome",
  "windows": ["dictate"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "dictate-overlay"
  ]
}
