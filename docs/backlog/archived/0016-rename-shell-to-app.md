---
id: "0016"
title: Rename Shell → App in code
status: done
---

# Rename Shell → App in code

- Keep `app-shell.svelte` filename (routing dependency)
- Rename component name, IPC events (`shell://navigate` → `app://navigate`), and comments to use App
